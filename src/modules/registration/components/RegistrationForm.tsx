import React, { FC } from 'react';

import { Form } from '../../core';

const RegistrationForm: FC = () => {
  return (
    <Form title="Registration" buttonLabel="Register" />
  );
};

export default RegistrationForm;
